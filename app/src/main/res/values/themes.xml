<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RestaurantBooking" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Font Type for the entire app -->
        <item name="android:fontFamily">@font/nunito_regular</item>
        <item name="fontFamily">@font/nunito_regular</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@android:color/holo_blue_light</item>
        <item name="colorPrimaryVariant">@color/teal_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/starYellow</item>
        <item name="colorControlActivated">@color/black</item>
    </style>

    <style name="Theme.RestaurantBooking.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Pound Sign Rating Barr -->
    <style name="CustomRatingBar" parent="RatingBar">
        <item name="android:progressDrawable">@drawable/pricebar</item>
        <item name="android:minHeight">18dp</item>
        <item name="android:maxHeight">20dp</item>
        <item name="colorControlNormal">#C5BCBC</item>
        <item name="colorControlActivated">@color/starYellow</item>
    </style>

    <style name="CustomToolbarTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

        <!-- HERE ARE THE CHANGES - Change color of different texts -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="Theme.RestaurantBooking.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.RestaurantBooking.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>